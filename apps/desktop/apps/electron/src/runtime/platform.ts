export type {
  Logger,
  ImageProcessor,
  PlatformServices,
} from '@sapstack-desktop/server-core/runtime'

export { createScopedLogger, CONSOLE_LOGGER } from '@sapstack-desktop/server-core/runtime'
