#!/bin/sh
ENTRY="${SAPSTACK_DESKTOP_COMMANDS_ENTRY:-$SAPSTACK_DESKTOP_CLI_ENTRY}"
export SAPSTACK_DESKTOP_CLI_JSON_ONLY="${SAPSTACK_DESKTOP_CLI_JSON_ONLY:-1}"
exec "${SAPSTACK_DESKTOP_BUN:-bun}" run "$ENTRY" "$@"
